#checkout@v19/simulated.void.transparency
#reveals hidden by water being used as a cast of the void of space.
#uses Light
#uses mass to gravity debinding by calculating weight>/<suns gravity on objects without calculating earth's gravity as the focus of objects within our atmosphere. Then copies code and decodes cloak. Oncommit(begin calculations for unbound cloak)
